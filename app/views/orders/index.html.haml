%h1
  = "#{@orders.count} Orders Total"

- if @orders.any?
  %h2
    = @orders.map{ |o| o.created_at.to_date == Date.today }.count
    Orders Today
  - @orders.each do |order|
    %p
      = l order.created_at
      %br
      = order.customer.email
    %br
- else
  Oops, doesn't look like there are any orders.
